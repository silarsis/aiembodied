{
  "name": "@aiembodied/main",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "ts-node-esm src/main.ts",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "electron": "^28.2.0",
    "keytar": "^7.9.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "ts-node": "^10.9.2"
  }
}
